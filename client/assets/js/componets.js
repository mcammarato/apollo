// Scripts here

$(document).ready(function(){

  // Carousel
  $('.carousel').slick({
    arrows: true,
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    accessibility: true,
    adaptiveHeight: true
  });

});
